#include "main.h"
/**
 * add - Add two intergers
 *
 * @a: first integer
 * @b: second integer
 * Return: (int) the result
 */
int add(int a, int b)
{
	int result =  0;
	/** add a to b and assign it to result*/
	result = a + b;
	return (result);
}
