#include "main.h"
#include <stdio.h>

/**
 * main - check the code.
 *
 * Return: Always 0.
 */
int main(void)
{
	char *str;
	
	str = "I don't fear computers. I fear the lack of them - Isaac Asimov";
	_puts(str);
	return (0);
}
