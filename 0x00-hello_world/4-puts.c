#include <stdio.h>
/**
 *main - Entry point
 *prints some strig to the output
 *put() writes a string to stdout
 * @myChat[] string to be printed
 *Return: Always 0 (Success)
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
