#include <stdio.h>
#include <stdlib.h>
#include "main2.h"
/**
 * mul - Find the multiple of two integers
 * @a: First integer
 * @b: Second integer
 *
 * Return: The Product of two integers
 */
int mul(int a, int b)
{
	return (a * b);
}
