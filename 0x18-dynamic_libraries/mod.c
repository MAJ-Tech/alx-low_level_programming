#include <stdio.h>
#include <stdlib.h>
#include "main2.h"
/**
 * mod - Find the mode of two integers
 * @a: First integer
 * @b: second integer
 *
 * Return: the mode of two integers
 */
int mod(int a, int b)
{
	return (a % b);
}
